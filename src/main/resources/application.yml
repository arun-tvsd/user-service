spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/my_db
    username: root
    password: toor
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update  # Or 'validate', 'create', 'none' depending on your needs
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
